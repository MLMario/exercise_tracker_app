---
phase: 04-workout-card-action-footer
plan: 01
type: execute
depends_on: []
files_modified: [apps/web/src/surfaces/workout/WorkoutExerciseCard.tsx, apps/web/css/styles.css]
---

<objective>
Move "+ Add Set" and "Remove" buttons from exercise-workout-card header to a new footer section at the bottom of the expanded card body.

Purpose: Cleaner card header with actions consolidated in a footer, matching the design mockup.
Output: Updated WorkoutExerciseCard component with action buttons in footer section.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@apps/web/src/surfaces/workout/WorkoutExerciseCard.tsx
@apps/web/css/styles.css

**Design reference:**
- new_design.png: Shows current layout with buttons in header (what to change FROM)
- remove_btn_down_exaple.png: Shows target layout with footer containing "+ Add Set" (left) and "Remove" (right)

**Current structure:**
- Header contains: progress ring, exercise name, "+ Add Set" button, remove button, chevron
- Body contains: set table, rest timer bar

**Target structure:**
- Header contains: progress ring, exercise name, chevron (cleaner)
- Body contains: set table, rest timer bar, NEW action footer with both buttons
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update WorkoutExerciseCard component structure</name>
  <files>apps/web/src/surfaces/workout/WorkoutExerciseCard.tsx</files>
  <action>
1. Remove the header-actions div (containing btn-add-set) from inside card-header
2. Remove the btn-remove button from inside card-header
3. Add a new "card-action-footer" div at the bottom of body-content (after RestTimerBar)
4. Move both buttons into card-action-footer:
   - btn-add-set on left (keep existing markup/handlers)
   - btn-remove on right (keep existing markup/handlers)
5. Remove e.stopPropagation() from button handlers since they're no longer in header
  </action>
  <verify>TypeScript compiles: cd apps/web && pnpm exec tsc --noEmit</verify>
  <done>Component restructured with buttons in footer, no TypeScript errors</done>
</task>

<task type="auto">
  <name>Task 2: Add CSS for card-action-footer</name>
  <files>apps/web/css/styles.css</files>
  <action>
1. Add new .exercise-workout-card .card-action-footer rule:
   - display: flex
   - justify-content: space-between
   - align-items: center
   - padding: 0.75rem
   - margin-top: var(--spacing-sm)
   - border-top: 1px solid var(--color-border-dim)
2. Update .btn-add-set styles (remove header-specific sizing, use footer-appropriate styles)
3. Update .btn-remove styles:
   - Remove opacity: 0 rule (always visible in footer)
   - Style as text button with icon: "Remove" text + trash icon
   - Use muted/danger color scheme matching mockup
4. Remove .exercise-workout-card .header-actions rules (no longer needed)
5. Remove .exercise-workout-card .card-header:hover .btn-remove opacity rule
  </action>
  <verify>App loads without CSS errors: cd apps/web && pnpm dev (check console)</verify>
  <done>Footer styled with buttons positioned left/right as shown in mockup</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `cd apps/web && pnpm exec tsc --noEmit` passes
- [ ] App runs without errors: `pnpm dev`
- [ ] Exercise card header shows only: progress ring, name, chevron
- [ ] Expanded card shows footer with "+ Add Set" (left) and "Remove" (right)
- [ ] Both buttons function correctly (add set works, remove exercise works)
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Card header is cleaner (no action buttons)
- Footer contains both action buttons
- Visual matches remove_btn_down_exaple.png mockup
- Phase 4 complete
</success_criteria>

<output>
After completion, create `.planning/phases/04-workout-card-action-footer/04-01-SUMMARY.md`
</output>
