---
phase: 12-exercise-picker-layout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/css/styles.css
autonomous: true

must_haves:
  truths:
    - "Category text appears below exercise name, not inline"
    - "Custom badge remains aligned to the right of the row"
    - "Long exercise names still truncate with ellipsis"
  artifacts:
    - path: "apps/web/css/styles.css"
      provides: "Column layout for exercise-item-info"
      contains: "flex-direction: column"
  key_links:
    - from: ".exercise-item-info"
      to: "name and category spans"
      via: "flex-direction: column"
      pattern: "flex-direction:\\s*column"
---

<objective>
Update exercise picker item layout to display category below exercise name instead of inline.

Purpose: Improve visual hierarchy by giving the exercise name its own line, with category as secondary information below it.
Output: Modified CSS in styles.css section 18 (Exercise Picker styles)
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-exercise-picker-layout/12-RESEARCH.md
@apps/web/css/styles.css (lines 2795-2831 - section 18 exercise picker styles)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CSS for stacked name/category layout</name>
  <files>apps/web/css/styles.css</files>
  <action>
Modify section 18 (Exercise Picker styles) to stack name and category vertically:

1. Update `.exercise-item-info` (around line 2796-2799):
   - Add `display: flex`
   - Add `flex-direction: column`
   - Add `gap: 2px` for small spacing between name and category
   - Keep existing `flex: 1` and `min-width: 0`

2. Update `.exercise-item-name` (around line 2802-2806):
   - Add `display: block` to ensure full-width
   - Add `white-space: nowrap`
   - Add `overflow: hidden`
   - Add `text-overflow: ellipsis`
   - Keep existing font-size, font-weight, color

3. Update `.exercise-item-category` (around line 2814-2817):
   - Add `display: block`
   - Keep existing font-size and color

Do NOT modify:
- `.exercise-list-item` - keep as horizontal flex with align-items: center
- `.badge-custom` - keep as-is for right alignment
- Any other styles in this section
  </action>
  <verify>
Run `npm run dev` in apps/web and verify in browser:
1. Open exercise picker modal (click + on any workout)
2. Exercise name appears on its own line
3. Category appears below name in smaller muted text
4. "Custom" badge (if visible) aligns to right of row
5. Long exercise names truncate with ellipsis, not wrap
  </verify>
  <done>
Category displays below exercise name with proper visual hierarchy. Badge alignment and text truncation work correctly.
  </done>
</task>

</tasks>

<verification>
Visual verification in browser:
1. Exercise picker modal shows name on first line
2. Category text appears below name
3. Custom badge (green pill) aligned to right
4. Layout works for both short and long exercise names
5. No visual regression on disabled (already-added) items
</verification>

<success_criteria>
- Category text is below exercise name (not inline)
- Custom badge remains properly aligned to the right
- Long exercise names truncate with ellipsis
- Gap between name and category is small (2px)
- No layout breaking on various exercise name lengths
</success_criteria>

<output>
After completion, create `.planning/phases/12-exercise-picker-layout/12-01-SUMMARY.md`
</output>
