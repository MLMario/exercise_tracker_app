---
phase: 30-root-cleanup
plan: 01
type: execute
depends_on: []
files_modified: [.gitignore]
---

<objective>
Remove unnecessary legacy folders from root after monorepo migration.

Purpose: Clean up stale files that are no longer needed after the v2.0 monorepo restructure.
Output: Clean root directory with only essential folders remaining.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

**What to remove:**
- `js/` - Contains only legacy `config.local.js` from pre-monorepo era (Supabase credentials now in `.env`)
- `dist/` - Stale build output; web app now builds to `apps/web/dist/`

**What to keep:**
- `apps/` - Monorepo applications (web, ios)
- `packages/` - Shared packages (@ironlift/shared)
- `node_modules/` - Root workspace dependencies (pnpm requires this)
- `sql/` - Database migration scripts (essential reference)
- `docs/` - Development documentation (gitignored, personal reference)
- `.claude/`, `.planning/`, `.vercel/`, `.git/` - Config/tooling directories
- Root config files (package.json, pnpm-workspace.yaml, tsconfig.json, vercel.json, .gitignore, .env*)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove legacy folders</name>
  <files>js/, dist/</files>
  <action>
    Delete `js/` folder entirely (contains only gitignored config.local.js).
    Delete root `dist/` folder entirely (stale build output; active build is at apps/web/dist/).

    Use `rm -rf` for both folders.
  </action>
  <verify>
    - `ls js/` returns "No such file or directory"
    - `ls dist/` at root returns "No such file or directory"
    - `ls apps/web/dist/` still exists (unaffected)
  </verify>
  <done>Both legacy folders removed from root</done>
</task>

<task type="auto">
  <name>Task 2: Update .gitignore</name>
  <files>.gitignore</files>
  <action>
    Remove the line `js/config.local.js` from .gitignore since the js/ folder no longer exists.
    Keep all other entries unchanged.
  </action>
  <verify>grep -c "js/config.local.js" .gitignore returns 0</verify>
  <done>.gitignore no longer references deleted js/ folder</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `ls -la` at root shows no `js/` or `dist/` folders
- [ ] `ls apps/web/dist/` still exists (web build unaffected)
- [ ] `.gitignore` has no reference to `js/config.local.js`
- [ ] `pnpm dev` still works (workspace not affected)
</verification>

<success_criteria>

- Both legacy folders (js/, dist/) removed
- .gitignore updated to remove obsolete entry
- Monorepo workspace functionality preserved
- Phase 30 complete, v2.1 Root Cleanup milestone complete
</success_criteria>

<output>
After completion, create `.planning/phases/30-root-cleanup/30-01-SUMMARY.md`
</output>
