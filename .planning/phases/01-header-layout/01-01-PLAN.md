---
phase: 01-header-layout
plan: 01
type: execute
depends_on: []
files_modified: [apps/web/css/styles.css]
---

<objective>
Fix template editor header layout so Cancel, title, and Save align in a single row.

Purpose: Ensure visual consistency with other surface headers (dashboard, workout).
Output: Template editor header displays `[Cancel] New Template [Save]` in proper alignment.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@apps/web/src/surfaces/template-editor/TemplateEditorSurface.tsx
@apps/web/css/styles.css

**Current state:**
- TemplateEditorSurface.tsx line 430-454: Header structure exists with Cancel btn, header-info div with title, Save btn
- `<div class="template-editor-surface">` wrapper exists but has no CSS
- `<header class="app-header">` has no styling for this surface
- `.header-info` class exists globally with `flex: 1` and centering (lines 261-265)

**Pattern to follow:**
- `.workout-header` (lines 234-258) uses `display: flex; justify-content: space-between; align-items: center`
- `.dashboard-surface .app-header` (lines 2477-2483) follows similar pattern

**Fix approach:**
Add `.template-editor-surface .app-header` CSS with flexbox layout matching existing patterns.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add template editor header CSS</name>
  <files>apps/web/css/styles.css</files>
  <action>
Add CSS for `.template-editor-surface .app-header` after the existing `.dashboard-surface .app-header` section (around line 2506).

CSS to add:
```css
/* Template Editor Surface Header */
.template-editor-surface .app-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  background-color: var(--color-bg-surface);
  border-bottom: 1px solid var(--color-border);
  padding: var(--spacing-md);
  position: sticky;
  top: 0;
  z-index: 100;
}
```

This matches the `.dashboard-surface .app-header` pattern exactly. The existing `.header-info` class (flex: 1, text-align: center) will properly center the title between the buttons.
  </action>
  <verify>Open template editor in browser and verify Cancel/Title/Save are in single row with title centered</verify>
  <done>Header displays as `[Cancel] New Template [Save]` in single row with title centered</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Template editor header layout with Cancel (left), Title (center), Save (right)</what-built>
  <how-to-verify>
    1. Run: pnpm dev (or npm run dev)
    2. Open browser to http://localhost:5173
    3. Log in and click "Create Template" to open template editor
    4. Verify header shows: [Cancel] centered title [Save] in single row
    5. Check Cancel button has secondary styling (gray/subtle)
    6. Check Save button has primary styling (accent color)
    7. Resize browser window to verify layout stays intact on mobile widths
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] CSS added to styles.css
- [ ] No TypeScript or build errors
- [ ] Header displays correctly in browser
</verification>

<success_criteria>

- Template editor header shows Cancel/Title/Save in single row
- Title is centered between buttons
- Layout works on desktop and mobile widths
- No regression in other surfaces
</success_criteria>

<output>
After completion, create `.planning/phases/01-header-layout/01-01-SUMMARY.md`
</output>
