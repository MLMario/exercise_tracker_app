---
phase: 19-auth-surface-rebrand
plan: 01
type: execute
depends_on: []
files_modified: [css/styles.css, src/surfaces/auth/AuthSurface.tsx]
---

<objective>
Update login/register UI to rebrand from "Ironlift Strength" to "IronFactor" with new visual design.

Purpose: Complete the visual rebrand on the auth surface - the first screen users see.
Output: Auth pages display "IronFactor" branding with split-color logo and tagline "Track. Train. Transform."
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.mockup/login-mockup-option-1.html

**Key files:**
@css/styles.css
@src/surfaces/auth/AuthSurface.tsx

**Design reference:**
The mockup shows:
- Brand section with "IronFactor" logo (Iron = white, Factor = accent blue)
- Tagline: "Track. Train. Transform."
- Larger card shadow (0 20px 60px rgba(0, 0, 0, 0.4))
- Font-weight 800 for logo, letter-spacing -0.02em
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add IronFactor brand logo styles to CSS</name>
  <files>css/styles.css</files>
  <action>
In Section 16 (Auth Flow), update the brand styles:

1. Update `.brand-title` to `.brand-logo` with new properties:
   - font-size: 3rem (3.5rem on tablet+)
   - font-weight: 800
   - letter-spacing: -0.02em
   - margin-bottom: var(--spacing-sm)

2. Add `.brand-logo .iron` class:
   - color: var(--color-text-primary) (white)

3. Add `.brand-logo .factor` class:
   - color: var(--color-accent) (blue)

4. Update `.brand-subtitle` to `.brand-tagline`:
   - font-size: 0.9375rem
   - font-weight: 400
   - color: var(--color-text-secondary)

5. Update `.auth-card` shadow to match mockup:
   - box-shadow: 0 20px 60px rgba(0, 0, 0, 0.4)

Keep existing `.brand-title` and `.brand-subtitle` classes as aliases (they're used elsewhere) - just add the new classes.
  </action>
  <verify>Open css/styles.css and confirm new classes exist: .brand-logo, .brand-logo .iron, .brand-logo .factor, .brand-tagline</verify>
  <done>CSS has IronFactor brand logo styles with split color classes and updated card shadow</done>
</task>

<task type="auto">
  <name>Task 2: Update AuthSurface with IronFactor branding</name>
  <files>src/surfaces/auth/AuthSurface.tsx</files>
  <action>
Update the AuthSurface component to add the brand section:

1. In the return statement, wrap everything in `auth-wrapper` div:

```tsx
return (
  <div class="auth-wrapper">
    {/* Brand Section */}
    <div class="brand">
      <h1 class="brand-logo">
        <span class="iron">Iron</span><span class="factor">Factor</span>
      </h1>
      <p class="brand-tagline">Track. Train. Transform.</p>
    </div>

    {/* Auth Card */}
    <div class="auth-card">
      {/* Existing tabs, surfaces, footer go here */}
    </div>
  </div>
);
```

2. Move existing `.auth-surface-container` content into the new `.auth-card` wrapper:
   - auth-tabs
   - success-message (if any)
   - surfaces-container
   - auth-footer

3. Remove the outer `.auth-surface-container` div (replaced by `.auth-card`)

Note: Do NOT change any logic, state, or functionality - only the JSX structure.
  </action>
  <verify>npm run build succeeds without errors</verify>
  <done>AuthSurface displays IronFactor brand logo above auth card with proper wrapper structure</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] CSS contains .brand-logo, .brand-logo .iron, .brand-logo .factor classes
- [ ] AuthSurface.tsx renders brand section with "Iron" and "Factor" spans
- [ ] AuthSurface.tsx uses auth-wrapper > brand + auth-card structure
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No TypeScript or build errors
- Auth page shows "IronFactor" with Iron in white, Factor in blue
- Tagline "Track. Train. Transform." visible below logo
</success_criteria>

<output>
After completion, create `.planning/phases/19-auth-surface-rebrand/19-01-SUMMARY.md`:

# Phase 19 Plan 01: Auth Surface Rebrand Summary

**[One-liner summarizing what shipped]**

## Accomplishments

- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified

- `path/to/file` - Description

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Ready for Phase 20: Dashboard Rebrand
</output>
