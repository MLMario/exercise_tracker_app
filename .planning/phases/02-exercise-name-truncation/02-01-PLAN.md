---
phase: 02-exercise-name-truncation
plan: 01
type: execute
depends_on: []
files_modified: [apps/web/src/surfaces/template-editor/ExerciseEditor.tsx]
---

<objective>
Add tooltip to exercise name in template editor so truncated names show full text on hover.

Purpose: Complete the truncation UX by ensuring users can see full exercise names when text is truncated with ellipsis.
Output: Exercise name span displays full name in native browser tooltip on hover.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-header-layout/01-01-SUMMARY.md

**Codebase context:**
- CSS truncation already in place: `.exercise-editor-card .exercise-name` has `white-space: nowrap; overflow: hidden; text-overflow: ellipsis;`
- Just need to add `title` attribute to the span for native browser tooltip

**Key file:**
@apps/web/src/surfaces/template-editor/ExerciseEditor.tsx

**Current state (line 132):**
```tsx
<span class="exercise-name">{exercise.name}</span>
```

**Target state:**
```tsx
<span class="exercise-name" title={exercise.name}>{exercise.name}</span>
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add title attribute to exercise name span</name>
  <files>apps/web/src/surfaces/template-editor/ExerciseEditor.tsx</files>
  <action>Add `title={exercise.name}` attribute to the exercise-name span on line 132. This enables native browser tooltip on hover to display full exercise name when text is truncated.</action>
  <verify>Open template editor with a long exercise name, hover over truncated name, tooltip displays full name</verify>
  <done>Exercise name span has title attribute, native tooltip appears on hover</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `pnpm build` in apps/web succeeds without errors
- [ ] Exercise name displays with ellipsis when too long
- [ ] Hovering over truncated name shows full name in tooltip
</verification>

<success_criteria>

- Task completed
- Build passes
- Native tooltip works on hover
- Phase 2 complete
</success_criteria>

<output>
After completion, create `.planning/phases/02-exercise-name-truncation/02-01-SUMMARY.md`
</output>
