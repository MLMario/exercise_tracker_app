---
phase: 18-template-list-redesign
plan: 01
type: execute
depends_on: []
files_modified: [css/styles.css, src/surfaces/dashboard/TemplateCard.tsx, src/surfaces/dashboard/TemplateList.tsx, src/surfaces/dashboard/DashboardSurface.tsx]
---

<objective>
Implement 2-column mini-grid layout for templates matching the mockup design.

Purpose: Improve space efficiency by fitting more templates in the viewport with a compact, modern aesthetic.
Output: Dashboard template section displays as 2-column mini-grid with compact cards and reduced visual clutter.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-template-list-redesign/18-CONTEXT.md

**Design reference:**
@.mockups/template-mockup-2-mini-grid.html

**Files to modify:**
@css/styles.css
@src/surfaces/dashboard/TemplateCard.tsx
@src/surfaces/dashboard/TemplateList.tsx
@src/surfaces/dashboard/DashboardSurface.tsx

**Key design requirements from mockup:**
- 2-column CSS grid layout (`templates-mini-grid`)
- Compact mini-cards with template name + edit/delete icon buttons in header
- Full-width Start button at bottom (`btn-xs` size)
- Icon buttons: 24x24px buttons with 12x12px SVG icons
- Template name limited to 2 lines with ellipsis
- Remove "X exercise(s) available" text from dashboard bottom
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add mini-grid CSS styles and restructure TemplateCard</name>
  <files>css/styles.css, src/surfaces/dashboard/TemplateCard.tsx</files>
  <action>
**CSS (css/styles.css):**
Add new mini-grid styles after the existing `.templates-grid` section (~line 2000):

1. `.templates-mini-grid` - 2-column CSS grid with `gap: var(--spacing-sm)`
2. `.template-mini-card` - Compact card with flexbox column layout
3. `.template-mini-header` - Flex row with name and action buttons
4. `.template-mini-name` - Font size 0.8125rem, 2-line clamp with ellipsis
5. `.template-mini-header-actions` - Flex row with 4px gap for icon buttons
6. `.btn-icon-xs` - 24x24px icon button with 12x12px SVG icons, transparent background, border
7. `.template-mini-actions` - Full-width container with `margin-top: auto`
8. `.btn-xs` - Extra small button variant (padding: 0.25rem 0.5rem, font: 0.6875rem)

Copy the exact CSS from mockup (lines 131-189 for mini-grid, lines 107-130 for btn-icon-xs, lines 102-106 for btn-xs).

**TemplateCard.tsx:**
Restructure to mini-card layout:
1. Change container from `.card .template-card` to `.template-mini-card`
2. Replace `.template-header` with `.template-mini-header` containing:
   - `.template-mini-name` span with template name
   - `.template-mini-header-actions` div with edit/delete buttons
3. Change icon buttons from `.btn .btn-icon` to `.btn-icon-xs`
4. Change delete button to also have `.btn-danger` class
5. Reduce icon SVG size from 18x18 to 12x12
6. Move Start button to `.template-mini-actions` div with classes `btn btn-primary btn-xs`
7. Remove the exercise count badge (no longer shown in mini-card design)
  </action>
  <verify>TypeScript compilation: `npm run build` succeeds without type errors</verify>
  <done>TemplateCard renders with mini-card structure, new CSS classes present in styles.css</done>
</task>

<task type="auto">
  <name>Task 2: Update TemplateList grid and remove exercise count</name>
  <files>src/surfaces/dashboard/TemplateList.tsx, src/surfaces/dashboard/DashboardSurface.tsx</files>
  <action>
**TemplateList.tsx:**
1. Change the grid container class from `templates-grid` to `templates-mini-grid` (line 57)
2. Update the Create Template button to use `btn btn-primary btn-sm` classes (matching mockup's "+ Create" style)

**DashboardSurface.tsx:**
1. Remove the entire "exercises info" section (lines 490-495):
   ```tsx
   {/* Exercises info - placeholder */}
   <section class="exercises-info">
     <p class="placeholder-text">
       {availableExercises.length} exercise(s) available
     </p>
   </section>
   ```
2. The `availableExercises` state is still needed for the AddChartModal, so keep the state and loading - just remove the display section.
  </action>
  <verify>`npm run build` succeeds, no TypeScript errors</verify>
  <done>TemplateList uses mini-grid class, Create button styled as btn-sm, exercise count text removed from dashboard</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>2-column mini-grid template layout matching mockup design</what-built>
  <how-to-verify>
    1. Run: `npm run dev`
    2. Visit: http://localhost:5173 (or dev server port)
    3. Log in to see the dashboard
    4. **Verify template grid:**
       - Templates display in 2-column grid layout
       - Each card shows: name (top-left) + edit/delete icons (top-right) + Start button (bottom, full-width)
       - Cards are compact with less vertical space than before
       - Long template names truncate with ellipsis after 2 lines
    5. **Verify styling:**
       - Edit icon button has border, shows hover effect
       - Delete icon button shows red on hover
       - Start button is full-width and smaller than before
    6. **Verify cleanup:**
       - "X exercise(s) available" text is NO LONGER visible at bottom of dashboard
    7. **Compare with mockup:** Open `.mockups/template-mockup-2-mini-grid.html` in browser and compare appearance
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `npm run build` succeeds without errors
- [ ] Template list displays as 2-column mini-grid
- [ ] Template cards match mockup design (name + icons in header, full-width Start button)
- [ ] Exercise count text removed from dashboard
- [ ] No console errors during development
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Visual layout matches `.mockups/template-mockup-2-mini-grid.html`
- Dashboard is cleaner with more templates visible in viewport
- Phase 18 complete
  </success_criteria>

<output>
After completion, create `.planning/phases/18-template-list-redesign/18-01-SUMMARY.md`:

# Phase 18 Plan 01: Template List Redesign Summary

**[One-liner describing what shipped]**

## Accomplishments

- [Key outcomes]

## Files Created/Modified

- `css/styles.css` - Added mini-grid CSS styles
- `src/surfaces/dashboard/TemplateCard.tsx` - Restructured to mini-card layout
- `src/surfaces/dashboard/TemplateList.tsx` - Updated grid class
- `src/surfaces/dashboard/DashboardSurface.tsx` - Removed exercise count display

## Decisions Made

[Any decisions or "None"]

## Issues Encountered

[Any issues or "None"]

## Next Step

Phase 18 complete. Check if Phase 19 is still needed or was absorbed into this phase.
</output>
