---
phase: 13-ui-polish
plan: 01
type: execute
depends_on: []
files_modified:
  - css/styles.css
  - src/surfaces/dashboard/ChartSection.tsx
  - src/surfaces/dashboard/DashboardSurface.tsx
  - index.html
---

<objective>
Polish UI elements for visual consistency and rebrand to "Ironlift Strength".

Purpose: Fix unstyled elements and improve visual appearance before release.
Output: Consistent button styling, proper layout, updated branding.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-ui-polish/CONTEXT.md
@css/styles.css
@src/surfaces/dashboard/ChartSection.tsx
@src/surfaces/dashboard/ChartCard.tsx
@src/surfaces/dashboard/DashboardSurface.tsx

**Prior context:**
- Phase 12 complete - bug fixes done
- App uses CSS custom properties for theming
- Button classes: .btn, .btn-primary, .btn-secondary, .btn-icon, .btn-ghost
- Modal class is .modal (but components use .modal-content - needs alignment)

**Styling gaps identified:**
- .chart-delete-btn - no styles
- .chart-card, .chart-card-header, .chart-card-title, .chart-card-content - no styles
- .modal-content - no styles (CSS has .modal instead)
- .logout-btn, .dashboard-header - no styles
- .section-header in ChartSection uses btn-icon with text span (incompatible)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Chart UI styling</name>
  <files>css/styles.css, src/surfaces/dashboard/ChartSection.tsx</files>
  <action>
1. In css/styles.css, add styles for chart components after the existing `.chart-container` and `.chart-title` rules (around line 700):

```css
/* Chart Card Styles */
.chart-card {
  background-color: var(--color-bg-surface);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-md);
  padding: var(--spacing-lg);
  transition: border-color var(--transition-fast);
}

.chart-card:hover {
  border-color: var(--color-accent);
}

.chart-card-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: var(--spacing-md);
}

.chart-card-title {
  font-size: 1rem;
  font-weight: 600;
  margin: 0;
  color: var(--color-text-primary);
}

.chart-card-content {
  position: relative;
  min-height: 200px;
}

.chart-loading {
  display: flex;
  align-items: center;
  justify-content: center;
  height: 200px;
  color: var(--color-text-muted);
}

.chart-canvas {
  width: 100%;
}

.chart-canvas.loading {
  opacity: 0;
}

/* Chart Delete Button */
.chart-delete-btn {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 32px;
  height: 32px;
  background-color: transparent;
  border: 1px solid var(--color-border);
  border-radius: var(--radius-sm);
  color: var(--color-text-secondary);
  cursor: pointer;
  transition: all var(--transition-fast);
}

.chart-delete-btn:hover {
  border-color: var(--color-danger);
  color: var(--color-danger);
  background-color: rgba(248, 113, 113, 0.1);
}
```

2. In ChartSection.tsx, fix the "Add Chart" button by removing btn-icon class and keeping proper button styling:
   - Change: `class="btn btn-secondary btn-icon"` → `class="btn btn-secondary"`
   - The btn-icon class forces 44x44 size which breaks when text is included

3. Add styles for the Add Chart modal by adding .modal-content rules that match .modal styling (or keep existing, just add backdrop):
```css
/* Modal Content - matches .modal but used by some components */
.modal-content {
  background-color: var(--color-bg-surface);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-lg);
  padding: var(--spacing-xl);
  max-width: var(--container-max-width);
  width: 100%;
  max-height: 90vh;
  overflow-y: auto;
  box-shadow: var(--shadow-lg);
  animation: slideUp var(--transition-normal);
}

/* Add Chart Modal - semi-transparent backdrop effect */
.add-chart-modal {
  background-color: rgba(26, 26, 26, 0.95);
  backdrop-filter: blur(10px);
}
```
  </action>
  <verify>Run `npm run dev` and visually inspect:
- Add Chart button has proper padding (not square)
- Chart delete (trash) button shows red on hover
- Add Chart modal has subtle transparency effect</verify>
  <done>Chart UI elements styled consistently with app design</done>
</task>

<task type="auto">
  <name>Task 2: Dashboard header and logout styling</name>
  <files>css/styles.css, src/surfaces/dashboard/DashboardSurface.tsx</files>
  <action>
1. In css/styles.css, add dashboard header styles after the modal section (around line 780):

```css
/* Dashboard Header */
.dashboard-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: var(--spacing-md);
  background-color: var(--color-bg-surface);
  border-bottom: 1px solid var(--color-border);
  position: sticky;
  top: 0;
  z-index: 100;
}

.dashboard-header h1 {
  font-size: 1.25rem;
  font-weight: 600;
  margin: 0;
  color: var(--color-accent);
}

/* Logout Button */
.logout-btn {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  padding: var(--spacing-sm) var(--spacing-md);
  font-size: 0.875rem;
  font-weight: 500;
  background-color: transparent;
  border: 1px solid var(--color-border);
  border-radius: var(--radius-md);
  color: var(--color-text-secondary);
  cursor: pointer;
  transition: all var(--transition-fast);
  min-height: 36px;
}

.logout-btn:hover {
  border-color: var(--color-danger);
  color: var(--color-danger);
  background-color: rgba(248, 113, 113, 0.1);
}
```

2. The DashboardSurface.tsx already has proper structure with .dashboard-header and .logout-btn - no changes needed to component.
  </action>
  <verify>Run `npm run dev`, navigate to dashboard:
- Header is sticky at top with dark background
- Title on left, logout button on right
- Logout button shows red hover state</verify>
  <done>Dashboard header properly positioned with styled logout button</done>
</task>

<task type="auto">
  <name>Task 3: Rebrand to "Ironlift Strength"</name>
  <files>index.html, src/surfaces/dashboard/DashboardSurface.tsx</files>
  <action>
1. In index.html line 6, change:
   `<title>FitTrack - Personal Fitness Tracker</title>` → `<title>Ironlift Strength</title>`

2. In DashboardSurface.tsx line 420, change:
   `<h1>Exercise Tracker</h1>` → `<h1>Ironlift Strength</h1>`

Note: The auth surface brand title uses "FitTrack" but per CONTEXT.md the rename is specifically for "Exercise Tracker" → "Ironlift Strength". If the auth brand should also change, update src/surfaces/auth/AuthSurface.tsx brand-title as well.
  </action>
  <verify>Run `npm run dev`:
- Browser tab shows "Ironlift Strength"
- Dashboard header shows "Ironlift Strength"</verify>
  <done>App rebranded to "Ironlift Strength" throughout</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `npm run build` succeeds without errors
- [ ] Add Chart button styled properly (not square, visible text)
- [ ] Chart delete button has hover state
- [ ] Add Chart modal has semi-transparent background
- [ ] Dashboard header sticky at top
- [ ] Logout button positioned right with hover state
- [ ] App title shows "Ironlift Strength" in browser tab and header
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No TypeScript errors
- UI elements match app's dark mode design language
- Phase 13: UI Polish complete
</success_criteria>

<output>
After completion, create `.planning/phases/13-ui-polish/13-01-SUMMARY.md`:

# Phase 13 Plan 01: UI Polish Summary

**[Substantive one-liner]**

## Accomplishments

- [Key styling changes]
- [Key layout fixes]
- [Branding update]

## Files Created/Modified

- `css/styles.css` - Added chart card, modal, dashboard header, logout button styles
- `src/surfaces/dashboard/ChartSection.tsx` - Fixed Add Chart button class
- `src/surfaces/dashboard/DashboardSurface.tsx` - Updated title (if needed)
- `index.html` - Updated page title

## Decisions Made

[Any styling decisions, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Phase 13 complete. v1.1 Fixes & Polish milestone complete.
</output>
