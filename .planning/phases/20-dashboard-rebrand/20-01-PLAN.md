---
phase: 20-dashboard-rebrand
plan: 01
type: execute
depends_on: []
files_modified: [css/styles.css, src/surfaces/dashboard/DashboardSurface.tsx]
---

<objective>
Update dashboard header with IronFactor branding to match auth surface styling.

Purpose: Complete the rebrand by applying consistent IronFactor branding across the app.
Output: Dashboard header displays "IronFactor" split-color logo matching the auth page pattern.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-auth-surface-rebrand/19-01-SUMMARY.md

**Pattern established in Phase 19:**
- `.brand-logo` container with `.iron` (white) + `.factor` (accent blue) spans
- Pattern: `<span class="brand-logo"><span class="iron">Iron</span><span class="factor">Factor</span></span>`

**Current state:**
- Dashboard header uses `<h1>Ironlift Strength</h1>` (line 435 of DashboardSurface.tsx)
- Current `.dashboard-header h1` styling: solid accent color

**Files to reference:**
@css/styles.css (lines 871-888 for dashboard header styles, lines for .brand-logo)
@src/surfaces/dashboard/DashboardSurface.tsx (lines 431-445 for header JSX)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dashboard header brand logo styles</name>
  <files>css/styles.css</files>
  <action>
Update `.dashboard-header h1` selector to style the brand logo for header context:
- Change selector from `.dashboard-header h1` to `.dashboard-header .brand-logo`
- Keep `font-size: 1.25rem`, `font-weight: 600`, `margin: 0`
- Remove `color: var(--color-accent)` (colors come from .iron/.factor classes)
- Add `letter-spacing: -0.01em` for tighter text in header

The existing `.brand-logo .iron` and `.brand-logo .factor` classes from Phase 19 will handle the split colors.
  </action>
  <verify>CSS file contains `.dashboard-header .brand-logo` selector with appropriate styles</verify>
  <done>Dashboard header brand logo CSS updated, no syntax errors</done>
</task>

<task type="auto">
  <name>Task 2: Update DashboardSurface header with IronFactor branding</name>
  <files>src/surfaces/dashboard/DashboardSurface.tsx</files>
  <action>
Replace the header h1 content (line ~435):

FROM:
```tsx
<h1>Ironlift Strength</h1>
```

TO:
```tsx
<span class="brand-logo">
  <span class="iron">Iron</span>
  <span class="factor">Factor</span>
</span>
```

This matches the pattern established in AuthSurface during Phase 19.
  </action>
  <verify>npm run build succeeds, no TypeScript errors</verify>
  <done>Dashboard header displays "IronFactor" with split-color styling</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `npm run build` succeeds without errors
- [ ] Dashboard header shows "IronFactor" with white "Iron" and blue "Factor"
- [ ] Header layout unchanged (logo left, logout button right)
- [ ] Visual consistency with auth page branding
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No TypeScript or CSS errors
- IronFactor branding consistent across auth and dashboard
- Phase 20 complete, v1.4 milestone ready to ship
</success_criteria>

<output>
After completion, create `.planning/phases/20-dashboard-rebrand/20-01-SUMMARY.md`
</output>
